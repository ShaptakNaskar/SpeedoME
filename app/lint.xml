<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!-- Adaptive icons stay in mipmap-anydpi-v26: aapt2 fails to resolve them from plain mipmap-anydpi in this setup. -->
    <issue id="ObsoleteSdkInt">
        <ignore path="src/main/res/mipmap-anydpi-v26" />
    </issue>
    <!-- targetSdk 36 is deliberate until Android 17 behaviour changes are reviewed (docs/plan.md, M9). -->
    <issue id="OldTargetApi" severity="ignore" />
    <!-- The battery-exemption request is an opt-in reliability upgrade behind BuildConfig.BATTERY_EXEMPTION_DIALOG (docs/plan.md §8, D17). -->
    <issue id="BatteryLife" severity="ignore" />
    <!-- A dependency ships Timber's lint rules; the app logs sparingly with android.util.Log and doesn't use Timber. -->
    <issue id="LogNotTimber" severity="ignore" />
</lint>
